{
  "type" : "function-prototype",
  "last-update" : "2024-01-16",
  "indices" : "core",
  "name" : "Core_InlinePointerHashMap_CompareKeysCallback",
  "id" : "core-inlinepointerhashmap-comparekeyscallback",
  "signature" : "typedef Core_Result (Core_InlinePointerHashMap_CompareKeysCallback)(Core_Boolean*, Core_InlinePointerHashMap_Key*, Core_InlinePointerHashMap_Key*);",
  "description" : "The type of a callback invoked to compare two keys.",
  "success": "@{code('*RETURN')} was assigned @{code('Core_True')} if the keys are equal and @{code('Core_False')} otherwise.",
  "parameters" : [
    {
      "name" : "RETURN",
      "type" : "@{code('Core_Boolean*')}",
      "description" : "A pointer to a @{code('Core_Boolean')} variable."
    },
    {
      "name" : "x",
      "type" : "@{code('Core_InlinePointerHashMap_Key*')}",
      "description" : "A pointer to a @{code('Core_InlinePointerHashMap_Key')} value."
    },
    {
      "name" : "y",
      "type" : "@{code('Core_InlinePointerHashMap_Key*')}",
      "description" : "A pointer to a @{code('Core_InlinePointerHashMap_Key')} value."
    }
  ],
  "errors" : [
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('RETURN')} is a null pointer."
    },
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('x')} is a null pointer."
    },
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('y')} is a null pointer."
    }
  ],
  "return" : "@{code('Core_Success')} on success. @{code('Core_Failure')} on failure.",
  "remarks" : "This function sets the by-thread error variable on failure."
}
