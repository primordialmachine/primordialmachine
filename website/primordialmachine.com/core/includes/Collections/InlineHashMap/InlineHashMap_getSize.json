{
  "type" : "function",
  "last-update" : "2024-02-07",
  "indices" : "core",
  "name" : "Core_InlineHashMap_getSize",
  "id" : "core-inlinehashmap-getsize",
  "signature" : "Core_Result Core_InlineHashMap&lt;Suffix&gt;_getSize(Core_Size* RETURN, Core_InlineHashMap&lt;Suffix&gt;* SELF);",
  "signatureRemarks": "<p>where <code>&lt;KeySuffix&gt;</code> <code>&lt;ValueSuffix&gt;</code> of <code>P</code>.</p>",
  "parameters" : [
    {
      "name" : "RETURN",
      "type" : "@{code('Core_Size*')}",
      "description" : "A pointer to a @{code('Core_Size')} variable."
    },
    {
      "name" : "SELF",
      "type" : "@{code('Core_InlineHashMap&lt;Suffix&gt;*')}",
      "description" : "A pointer to a @{code('Core_InlineHashMap&lt;Suffix&gt;')} object."
    }
  ],
  "success": "@{code('*RETURN')} was assigned the size, in elements, of this @{code('Core_InlineHashMap&lt;Suffix&gt;')} object.",
  "errors" : [
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('RETURN')} is a null pointer."
    },
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('SELF')} is a null pointer."
    }
  ],
  "description" : "Get the size, in elements.",
  "return" : "@{code('Core_Success')} on success. @{code('Core_Failure')} on failure.",
  "remarks" : [
    "This function sets the by-thread error variable on failure.",
    "This function fails only with the specified errors/error conditions."
  ]
}
