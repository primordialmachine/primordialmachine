{
  "type" : "enumeration",
  "last-update" : "2023-12-26",
  "indices" : "val",
  "name" : "Core_DepthCompareFunction",
  "id" : "core-depthcomparefunction",
  "signature" : "typedef enum Core_DepthCompareFunction &lt;implementation&gt; Core_DepthCompareFunction;",
  "description": "An enumeration of depth compare functions. A depth compare function identifies a technique that compares depth value of an incoming fragment to its corresponding depth value in the depth buffer.",
  "elements": [
    {
      "name" : "Core_DepthCompareFunction_Always",
      "description" : "The incoming fragment always passes."
    },
    {
      "name" : "Core_DepthComparefunction_Never",
      "description" : "The incoming fragment never passes."
    },
    {
      "name" : "Core_DepthCompareFunction_Equal",
      "description" : "The incoming fragment passes if its depth value is equal to its corresponding depth value in the depth buffer."
    },
    {
      "name" : "Core_DepthCompareFunction_NotEqual",
      "description" : "The incoming fragment passes if its depth value is not equal to its corresponding depth value in the depth buffer."
    },
    {
      "name" : "Core_DepthCompareFunction_LessThan",
      "description" : "The incoming fragment passes if its depth value is less than its corresponding depth value in the depth buffer."
    },
    {
      "name" : "Core_DepthCompareFunction_LessThanOrEqualTo",
      "description" : "The incoming fragment passes if its depth value is less than or equal to its corresponding depth value in the depth buffer."
    },
    {
      "name" : "Core_DepthCompareFunction_GreaterThan",
      "description" : "The incoming fragment passes if its depth value is greater than its corresponding depth value in the depth buffer."
    },
    {
      "name" : "Core_DepthComparefunction_GreaterThanOrEqualTo",
      "description" : "The incoming fragment passes if its depth value is greater than or equal to its coressponding depth value in the depth buffer."
    }
  ]
}
