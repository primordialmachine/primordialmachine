{
  "type" : "function",
  "last-update" : "2024-01-10",
  "indices" : "core",
  "name" : "Core_InlineArray_getSize",
  "id" : "core-inlinearray-getsize",
  "signature" : "Core_Result Core_InlineArray&lt;Type&gt;_getSize(Core_Size* RETURN, Core_InlineArray&lt;Type&gt;* SELF);",
  "signatureRemarks": [
    "<p>where <code>&lt;Suffix&gt;</code> and <code>&lt;Type&gt;</code> can assume the values of</p>",
    "<table style='width: initial !important'>",
    "<tr><td><code>&lt;Suffix&gt;</code></td> <td><code>&lt;Type&gt;  </code></td></tr>",
    "<tr><td><code>N8            </code></td> <td><code>Core_Natural8 </code></td></tr>",
    "<tr><td><code>P             </code></td> <td><code>void*         </code></td></tr>",
    "</table>"
  ],
  "parameters" : [
    {
      "name" : "RETURN",
      "type" : "@{code('Core_Size*')}",
      "description" : "A pointer to a @{code('Core_Size')} variable."
    },
    {
      "name" : "SELF",
      "type" : "@{code('Core_InlineArray&lt;Type&gt;*')}",
      "description" : "A pointer to a @{code('Core_InlineArray&lt;Type&gt;')} object."
    }
  ],
  "success": "@{code('*RETURN')} was assigned the size, in elements, of this @{code('Core_InlineArray&lt;Type&gt;')} object.",
  "errors" : [
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('RETURN')} is a null pointer."
    },
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('SELF')} is a null pointer."
    }
  ],
  "description" : "Get the size, in elements, of a @{code('Core_InlineArray&lt;Type&gt;')} object.",
  "return" : "@{code('Core_Success')} on success. @{code('Core_Failure')} on failure.",
  "remarks" : [
    "This function sets the by-thread error variable on failure.",
    "This function fails only if @{code('SELF')} or @{code('RETURN')} is a null pointer."
  ]
}
