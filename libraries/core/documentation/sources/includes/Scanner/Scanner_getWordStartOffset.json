{
  "type" : "method",
  "last-update" : "2024-02-07",
  "indices" : "core",
  "name" : "Core_Scanner_getWordStartOffset",
  "id" : "core-scanner-getwordstartoffset",
  "signature" : "Core_Result Core_Scanner_getWordStartOffset(Core_Size* RETURN, Core_Scanner* SELF);",
  "parameters" : [
    {
      "name" : "RETURN",
      "type" : "@{code('Core_Size*')}",
      "description" : "A pointer to a @{code('Core_Size')} variable."
    },
    {
      "name" : "SELF",
      "type" : "@{code('Core_Scanner*')}",
      "description" : "A pointer to this @{code('Core_Scanner*')} object."
    }
  ],
  "success": "@{code('*RETURN')} was assigned the offset, in Bytes, at which the current word starts.",
  "errors" : [
    {
      "errorCode" : "Core_Error_ArgumentInvalid",
      "errorCondition" : "@{code('SELF')} is a null pointer."
    }
  ],
  "description" : "Get the offset, in Bytes, at which the current word starts.",
  "return" : "@{code('Core_Success')} on success. @{code('Core_Failure')} on failure.",
  "remarks" : "This function sets the by-thread error variable on failure."
}
